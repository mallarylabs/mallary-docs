---
title: "Get Job"
description: "Retrieve job status, result payload, and user warnings."
openapi: "GET /api/v1/jobs/{id}"
---

When the platform returns or exposes them, completed publishing jobs include normalized `platform_post_id` and `platform_post_url` fields. The raw provider result remains available in `data.job.result`.

---
title: "Quickstart"
description: "Get started with health checks, uploads, posting, connected-platform listing, and job inspection."
---

## Health Check

```bash
mallary health
```

## Upload Local Media

```bash
mallary upload ./launch.mp4
```

## Create a Post

```bash
mallary posts create \
  --message "Check out my new product video!" \
  --platform facebook \
  --platform instagram \
  --profile-id AbC123xYz90 \
  --media ./launch.mp4 \
  --thumbnail ./launch-cover.jpg
```

## List Posts

```bash
mallary posts list
```

## Inspect a Job

```bash
mallary jobs get 123
```

## List Connected Platforms

```bash
mallary platforms list
```

## List Your Profiles within Mallary

Profiles are used to group your social media accounts. You can create a profile for each of your businesses, and then connect your social media accounts for each business inside this profile. Your default profile will be used if you don’t pass a `profile_id` when making requests.

Each profile has a random public ID, such as AbC123xYz90. Use that ID as `profile_id` in API, MCP, and CLI requests.

Omit `profile_id` on publishing and reporting endpoints to use the default profile. Send `profile_id` when you want to target a non-default profile.

```bash
mallary profiles list
```
